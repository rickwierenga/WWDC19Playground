/*:
 # PictionARy

 Press run to build the code. The instructions will be presented in the playground.
 
 Tips to get more points:
 * Draw big
 * Use a black felt tip
 * Be simple (really simple)
 * Have a good lighting
 
 And:
 * Turn your sound on
 * Be creative
 
 __Good luck!__
 */

// Get the goodies 😋
import UIKit
import PlaygroundSupport

// Let's set up the view really quick ...
let navigationController = UINavigationController(rootViewController: IntroductionViewController())
navigationController.isNavigationBarHidden = true
PlaygroundPage.current.liveView = navigationController
